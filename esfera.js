const pi = 3.14159
const R1 = 3
const R2 = 15
const R3 = 1523

const volume1 = (4.0 / 3) * pi * (R1 * R1 * R1)
const volume2 = (4.0 / 3) * pi * (R2 * R2 * R2)
const volume3 = (4.0 / 3) * pi * (R3 * R3 * R3)

console.log(`VOLUME = ${volume3.toFixed(3)}`);
